name = "mizchi/blas"

version = "0.1.3"

description = "BLAS bindings for MoonBit (Accelerate/OpenBLAS)"

readme = "README.md"

repository = "https://github.com/mizchi/blas"

license = "MIT"

keywords = [ "blas", "linear-algebra", "matrix", "native" ]

source = "src"

supported_targets = "native"

preferred_target = "native"
