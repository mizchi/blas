import {
  "mizchi/blas" @blas,
  "moonbitlang/core/env" @env,
  "moonbitlang/core/strconv" @strconv,
}

options(
  "is-main": true,
  // Link flags must be provided by CI or user:
  // macOS: "-framework Accelerate"
  // Linux: "-lopenblas -llapack -lm"
  targets: { "main.mbt": [ "native" ] },
)
