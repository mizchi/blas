options(
  link: {
    "native": {
      // macOS: use Accelerate.framework
      // Linux: override with MOON_CC_LINK_FLAGS="-lopenblas -lm"
      "cc-link-flags": "-framework Accelerate",
    },
  },
  "native-stub": [ "blas_stub.c" ],
  "supported-targets": [ "native" ],
)
